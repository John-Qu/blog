if (file.exists("~/.Rprofile")) {
  base::sys.source("~/.Rprofile", envir = environment())
}
options(
  blogdown.method = 'custom', blogdown.yaml.empty = FALSE,
  blogdown.themesDir = '../..', blogdown.subdir = "cn" 
)

